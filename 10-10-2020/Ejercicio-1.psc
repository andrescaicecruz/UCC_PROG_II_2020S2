Proceso ejercicio_en_clase_1
	//defino variable 
	definir a, b, x, yy Como Entero;
	definir s Como Logico;
	a <- 1; b <- 0; x <- 0; yy <- 2;
	
	s <- (a > b) y (x = yy);
	
	escribir "el valor final de la variable s es;", s;
FinProceso